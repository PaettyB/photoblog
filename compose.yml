services:
  astro:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: astro-photo-blog
    ports:
      - "4321:4321"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      CHOKIDAR_USEPOLLING: "true"
    stdin_open: true
    tty: true